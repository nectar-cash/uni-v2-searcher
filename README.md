# Simple arbitrage searcher backrunner

Simple backrunning searcher, takes transaction from mempool and evaluates if it'll be profitable to backrun it. 

```shell
yarn
npx hardhat run src/run-searcher.ts
```
